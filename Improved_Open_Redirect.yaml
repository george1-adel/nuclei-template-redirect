id: open-redirect-generic

info:
  name: Open Redirect - Detection
  author: afaq,melbadry9,Elmahdi,pxmme1337,Regala_,andirrahmani1,geeknik
  severity: medium
  description: An open redirect vulnerability was detected.
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N
    cvss-score: 6.1
    cwe-id: CWE-601
  tags: redirect,generic,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/{{redirect}}"
      - "{{BaseURL}}/?redirect={{redirect}}"

    redirects: false
    payloads:
      redirect: redirect.txt

    stop-at-first-match: true

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 300
          - 301
          - 303
          - 302
          - 307
          - 308
        condition: or

      - type: regex
        part: header
        condition: or
        regex:
          - '(?im)^\s*location\s*:\s*(?:https?://|//)(?:[A-Za-z0-9](?:[A-Za-z0-9._-]*[A-Za-z0-9])?\.)*(?:oast\.me|google\.com|example\.com|whitelisteddomain\.tld|attacker\.com|evil\.com)(?::\d+)?(?:[/?#].*)?$'
          - '(?im)^\s*location\s*:.*?(?:j.*a.*v.*a.*s.*c.*r.*i.*p.*t|alert\(.*?\)|confirm\(.*?\)|prompt\(.*?\))'

    extractors:
      - type: regex
        part: header
        name: location_value
        regex:
          - '(?im)^location\s*:\s*(https?://|//)?([^\r\n]+)'
